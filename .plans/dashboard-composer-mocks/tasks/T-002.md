# T-002: Mover ModelSelector al Composer

## Objetivo
Mover el `ModelSelector` del `ChatHeader` al toolbar del `ComposerToolbar`, posicionado como pill/badge sobre el textarea.

## Archivos a modificar

### `src/components/ChatHeader.tsx`
- Eliminar la importación de `ModelSelector`
- Eliminar el div derecho que contiene `<ModelSelector sessionId={sessionId} />`
- Dejar solo el título editable y los status indicators

El header debe quedar más limpio:
```tsx
<div className="flex items-center justify-between ...">
  <div className="flex items-center gap-3 min-w-0 flex-1">
    {isEditing ? <input ... /> : <button onClick={...}>{title}</button>}
    <StatusDot status={...} streaming={...} />
    <StatusLabel status={...} streaming={...} />
    {streaming.isCompacting && <span>Compacting</span>}
    {hasQueue && <span>{queueCount} queued</span>}
  </div>
  {/* No more ModelSelector here */}
</div>
```

### `src/components/composer/ComposerToolbar.tsx`
- Importar `ModelSelector`
- Renderizarlo dentro del toolbar:
```tsx
<div className="flex items-center gap-1 shrink-0 pb-0.5">
  <ModelSelector sessionId={sessionId} />
</div>
```

### `src/components/ModelSelector.tsx` (ajustes)
Puede necesitar ajustes menores para integrarse mejor en el composer:
- Estilo más compacto (el header tiene h-14, el composer tiene menos espacio vertical)
- Posicionamiento del dropdown: si está cerca del borde inferior, el dropdown debe abrirse hacia arriba
- Considerar agregar prop `dropdownPosition?: "up" | "down"` por si el comportamiento por defecto no es óptimo

```typescript
interface ModelSelectorProps {
  sessionId: string;
  dropdownPosition?: "up" | "down";
}
```

## Notas de diseño
- El ModelSelector debe verse como un badge/pill integrado en el toolbar del composer
- Estilo compacto: mismo padding que los botones de acción del composer
- El dropdown debe abrirse hacia arriba cuando el composer está cerca del borde inferior de la pantalla
- El CSS actual de `.absolute.right-0.top-full` debe cambiar condicionalmente a `.bottom-full` cuando sea necesario
- Mantener la agrupación por provider y el checkmark de selección

## Criterios de éxito
- [ ] ChatHeader no tiene ModelSelector
- [ ] ComposerToolbar tiene ModelSelector
- [ ] ModelSelector funciona igual que antes (dropdown, grouped providers, selection)
- [ ] Dropdown se posiciona correctamente (hacia arriba si está cerca del borde)
- [ ] Estilos consistentes con el resto del composer
- [ ] `bun run build` pasa
- [ ] `bun run test` pasa
