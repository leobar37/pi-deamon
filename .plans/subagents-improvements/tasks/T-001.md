# T-001: Remove Dead Code

## Objective
Remove code that is never executed or consumed, reducing package surface and cognitive load.

## Files to Modify

### Remove completely
- `src/event-core.ts` (lines 51-112: `TypedEventBus` class) - 62 lines, zero consumers, exported in public API
- `src/instructions/presets.ts` (entire file: `withSummary`, `bulletList`, `onlyFlagSecurity`, `minimalChanges`) - zero external consumers
- `src/transport/dashboard-html.ts` (entire file: `DASHBOARD_HTML` constant) - never imported
- `src/execution/` directory (chain.ts, sequential.ts, parallel.ts, execute.ts, index.ts) - 5 files, System A barely consumed
- `src/execution/index.ts` re-exports from package index

### Fix references
- `src/index.ts` - remove exports for dead code:
  - Remove `TypedEventBus` export
  - Remove `execute`, `executeChain`, `executeSequential`, `executeParallel` exports
  - Remove preset exports (`withSummary`, `bulletList`, `onlyFlagSecurity`, `minimalChanges`)
  - Remove commented-out export on line 30
- `src/event-defs.ts` - remove `createEvent` usage since it comes from `event-core.ts` (but keep event type definitions - ref T-002)
  - Actually, T-002 handles this. For T-001 just remove TypedEventBus and its export.
- `src/controller.ts` - update `executePlan()` to NOT use System A execution functions (stub or redirect to T-003)

### Keep but update
- `src/event-core.ts` - keep `createEvent()` factory function (handled in T-002), only remove `TypedEventBus`

## Files Affected
- Delete: `src/transport/dashboard-html.ts`, `src/instructions/presets.ts`
- Delete: `src/execution/chain.ts`, `src/execution/sequential.ts`, `src/execution/parallel.ts`, `src/execution/execute.ts`, `src/execution/index.ts`
- Modify: `src/index.ts` (remove dead exports)
- Modify: `src/event-core.ts` (remove TypedEventBus)
- Modify: `src/controller.ts` (update executePlan reference)

## Acceptance Criteria
- `grep -rn "TypedEventBus" src/` returns only the definition file (after removal, nothing)
- `grep -rn "DASHBOARD_HTML" src/` returns nothing
- `grep -rn "withSummary|bulletList|onlyFlagSecurity|minimalChanges" src/ --include="*.ts" | grep -v test | grep -v ".d.ts"` returns nothing
- `find src/execution -type f` returns no files
- All existing tests still pass
- `bun run check` passes

## Validation
1. Run `grep` for removed symbols to confirm they're gone
2. Run `bun run check` for type errors
3. Run all existing tests
