# T-008: Test Frontend

## Objective
Add test coverage for the React frontend: components, hooks, and Zustand stores. Currently zero tests.

## Priority

### 1. Zustand Stores
**`src/store/subagent-store.ts`**: Test event accumulation, state updates from SSE, data merging.
**`src/store/session-messages-store.ts`**: Test message append, clear, pagination.

### 2. Hooks
**`src/hooks/use-sse.ts`**: Test connection management, event parsing, reconnect, error handling (with mock EventSource).
**`src/hooks/use-auto-scroll.ts`**: Test scroll behavior with mocked DOM.

### 3. Components (smoke tests)
- `App.tsx`: renders without crash
- `AgentDetail.tsx`: renders thread view, handles loading/error/empty states
- `ChatView.tsx`: renders messages correctly (user, assistant, tool, system variants)
- `EventBubble.tsx`: renders all 10+ event types
- `StatusBadge.tsx`: renders all 10+ status variants
- `SubagentRunBlock.tsx`: renders with/without child subagents
- `useMessageRenderers.tsx`: renders thinking, tool calls, images, code blocks

## Test Approach
- Use Vitest + @testing-library/react
- Use jsdom environment (already in vitest.config.ts if not, add it)
- Create mock data factories (separate from `src/mocks/data.ts`)
- Mock Zustand stores for component tests
- Mock EventSource for SSE hook tests

## Files to Create
- `frontend/src/__tests__/subagent-store.test.ts`
- `frontend/src/__tests__/session-messages-store.test.ts`
- `frontend/src/__tests__/use-sse.test.ts`
- `frontend/src/__tests__/AgentDetail.test.tsx`
- `frontend/src/__tests__/ChatView.test.tsx`
- `frontend/src/__tests__/EventBubble.test.tsx`
- `frontend/src/__tests__/StatusBadge.test.tsx`

## Acceptance Criteria
- Store tests: verify state transitions, event accumulation, data merging
- Hook tests: verify SSE connection lifecycle, event parsing
- Component tests: verify renders, empty/loading/error states, snapshots for stable components
- All tests pass in headless mode

## Validation
1. Run frontend tests with `npx vitest --run`
2. Verify existing functionality is not broken
3. Check test coverage report
