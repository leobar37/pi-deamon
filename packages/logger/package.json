{
  "name": "@local/pi-logger",
  "version": "0.0.1",
  "description": "Structured session logger with JSONL persistence",
  "type": "module",
  "private": true,
  "keywords": ["pi-package"],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "peerDependencies": {
    "@types/node": "*"
  }
}
